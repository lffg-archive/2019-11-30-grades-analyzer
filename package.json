{
  "private": true,
  "name": "@lffg/grades-analyzer",
  "description": "Grades Analyzer",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "yarn lint && del dist && tsc",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "lint:fix": "yarn lint --fix"
  },
  "dependencies": {
    "del-cli": "^1.1.0",
    "eslint": "^5.16.0",
    "eslint-config-lffg": "^1.2.0",
    "fs-extra": "^8.0.1",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.5"
  },
  "devDependencies": {
    "@types/fs-extra": "^7.0.0",
    "@types/node": "^12.0.2"
  }
}
